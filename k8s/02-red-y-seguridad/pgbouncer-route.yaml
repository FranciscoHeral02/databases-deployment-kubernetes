apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: pgbouncer-ssl
  namespace: databases
spec:
  entryPoints:
    - postgres
  routes:
  - match: HostSNI(`db.devfheral.dev`)
    services:
    - name: pgbouncer-external
      port: 6432
  tls:
    secretName: wildcard-tls
